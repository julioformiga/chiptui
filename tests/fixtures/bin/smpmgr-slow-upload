#!/bin/sh
# Fake `smpmgr` whose upload takes a minute: the Stop-mid-upload shape.
# The behavior lives in `smpmgr` proper; this wrapper only selects it (per
# process, so parallel tests never share it).
exec env SMPMGR_FAKE_SLOW_UPLOAD=1 "$(dirname "$0")/smpmgr" "$@"
