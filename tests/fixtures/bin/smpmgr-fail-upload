#!/bin/sh
# Fake `smpmgr` whose link dies mid-upload: `image upload` fails partway.
# The behavior lives in `smpmgr` proper; this wrapper only selects it (per
# process, so parallel tests never share it).
exec env SMPMGR_FAKE_FAIL_UPLOAD=1 "$(dirname "$0")/smpmgr" "$@"
